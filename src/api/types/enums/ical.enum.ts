/**
 * @description ICal endpoint sources /external/ical/:source/:api
 */
export enum SOURCE {
  checkup = 'checkup',
  treatment = 'treatment',
  feed = 'feed',
  harvest = 'harvest',
  movedate = 'movedate',
  rearing = 'rearing',
  scale_data = 'scale_data',
  todo = 'todo'
}
